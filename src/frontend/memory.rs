// Holds the components for rendering a table of the memory
use dioxus::prelude::*;

fn MemoryTable(cx: Scope) -> Element {
    cx.render(rsx! {
        table {

        }
    })
}

fn MemoryRow(cx: Scope) -> Element {
    cx.render(rsx! {
        tr {

        }
    })
}
